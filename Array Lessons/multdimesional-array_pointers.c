#include <stdio.h>
#include <string.h>

/**
 * main - printing multi-dimensional arrays using pointers
 * Return: Always zero
*/

int main()
{
    char Hello[] = "You know the name";
    int size ;
    size = strlen(Hello);
    printf("%d", size); 
    return 0;
}