// The var keyword is used to declare a variable
var x;
x = 34;
//console.log() is the function used to print to STDOUT
///console.log(x);

// Just like the var, let keyword can also be used to declare variables
let y = 90;

//console.log(y);
//console.log(x+y);

console.log("John" + " " + "Amponsah")

var x = 8;
console.log(x**y);
let result = x < y;
console.log(result);
console.log(typeof(result));

//using the const keyword to create an array. 
// Arrays are denoted by [] brackets and are accessed by index starting from  [0]
const people = ["Ama", "Yaw"];

//creating objects

const car = {type: "Honda", model: "Civic", Year: "2005"};

// to access a property of an object use objectName.propertyName;

// ie: car.type;
//to access a method of an object use objectName.methodName();